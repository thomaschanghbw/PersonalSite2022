import BlogPostLayout from "components/layout/blogPost/BlogPostLayout";
import ThousandWordsTextArea
    from "components/thousandwords/ThousandWordsTextArea";

export const meta = {
author: 'Rich Haines',
    title: "My MDX Page with a Layout",
    date: new Date('Feb 17, 2022 '),
    abstract: "In this article I am going to bananas"
}

# My MDX Page with a Layout

This is a list in markdown:

- One
- Two
- Three

## Heading 2
Checkout my React component:

<ThousandWordsTextArea />

```python
x = [1,2,3]
for num in x:
    print(num)
```

export default ({ children }) => <BlogPostLayout meta={meta}>{children}</BlogPostLayout>
